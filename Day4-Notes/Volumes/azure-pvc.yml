apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-managed-disk
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: managed-premium
  resources:
    requests:
      storage: 5Gi
